# flotilla.social

Marketing site and help center for [Flotilla](https://flotilla.social), a Nostr-based
community and chat app. Built with Astro; help articles live in `src/content/articles/`.

```sh
pnpm install   # install dependencies
pnpm dev       # local dev server at localhost:4321
pnpm build     # production build to ./dist/
```
